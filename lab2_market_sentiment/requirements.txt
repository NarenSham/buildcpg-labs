# Install these in order to avoid pyarrow issues
# This is specifically for Intel Mac + macOS 11+

# First: Pre-built packages
numpy==1.24.3
pandas==2.1.3

# DuckDB (usually works fine)
duckdb==0.9.1

# dbt (doesn't need compilation)
dbt-core==1.7.0
dbt-duckdb==1.7.0

# Streamlit + deps
streamlit==1.28.1
plotly==5.17.0
pyarrow==13.0.0  # Specific version, more likely to have pre-built wheel

# Optional: For real data
praw==7.7.0
requests==2.31.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1